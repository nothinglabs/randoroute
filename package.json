{
  "name": "clauding-ios-shell",
  "version": "1.0.0",
  "private": true,
  "description": "Personal Capacitor iOS wrapper for the Clauding web app.",
  "scripts": {
    "ios:add": "cap add ios",
    "ios:prepare-shell": "node scripts/build_mobile_shell.mjs",
    "ios:sync": "npm run ios:prepare-shell && cap sync ios",
    "ios:open": "cap open ios",
    "ios:build": "npm run ios:prepare-shell && cap build ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.4.2",
    "@capacitor/ios": "^8.4.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.2"
  }
}
